# Whitelist mode: everything is ignored by default; only the file types
# below (in any directory) are tracked. A new notebook, slide deck or
# script of an allowed type shows up automatically — no per-file edits
# needed here. Anything not explicitly allowed stays local-only.
*
!*/
!*.ipynb
!*.py
!*.sh
!*.pptx
!*.md
!*.toml
!uv.lock
!*.txt
!.gitignore
!LICENSE

# Local-only build artifacts — share an allowed extension above, so they
# need to be re-ignored explicitly (slide generators and class scripts).
build_slides.py
script.md

# Logística interna del aula — no es material del curso, no se sube.
CHECKLIST_AULA.md

# Notas de trabajo local por bloque, para iterar contenido — nunca se sube.
correcciones.txt

# Solo sirve para generar las presentaciones (usado por build_slides.py,
# que ya es local-only) — no es material del curso, no se sube.
_shared/

# Data and regenerable outputs — never in the repo, except explicit
# allow-listed precomputed caches too expensive to regenerate (see below).
data/
data_bruto/
results/*
!results/ironmarch/
results/ironmarch/*
!results/ironmarch/ner_results.parquet

# Junk / environment — would otherwise be dragged in by !*/.
__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/
.venv/
env/
.DS_Store
Thumbs.db
_archive/
logs/
.atl/
